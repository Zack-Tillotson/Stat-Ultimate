  <header class="site-header">

    <div class="site-info">
      <a href="/" class="header-container">
        <div class="header-top">ULTIMATE</div>
        <div class="header-bottom">BOX SCORE</div>
      </a>
    </div>

    <div class="nav-items">
      <% if @user and @user.id %>
        <%=link_to "My Teams", teams_path(), :class => "nav-link" %>
      <%end%>
      <% if @team and @team.id %>
        <%=link_to @team.name, team_path(@team), :class => "nav-link" %>
      <% end %>
    </div>


  </header>
